#include <stdio.h>
/**
 * main - function
 * description - alphabet
 * return: 0 (success);
 */

int main(void)
{
	char abc = 'a';

	for (abc = 'a'; abc <= 'z'; abc++)
	putchar(abc);
	putchar('\n');
	return (0);
}
