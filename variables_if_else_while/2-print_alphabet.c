#include <stdio.h>
#include <stdlib.h>
/**
 * main - function
 * description - in the alphabet
 * Return: 0
 */
int main(void)
{
	char abc = 'a';

	for (abc = 'a'; abc <= 'z'; abc++)
	putchar(abc);
	putchar('\n');
	return (0);
}
