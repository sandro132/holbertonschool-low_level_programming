#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result.
 * @s1: first number
 * @s2:second number
 * Return: suma1+suma2
 */

int add(int s1, int s2)
{

	return (s1 + s2);
}
